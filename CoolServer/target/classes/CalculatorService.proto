syntax = "proto3";

package  com.test.grpc;

message Request {
  double startTime = 1;
  double endTime = 2;
  double step = 3;
}

message Response {
  double time = 1;
  double result = 2;
}

service Calculator {
  rpc calculate(Request) returns (stream Response);
}